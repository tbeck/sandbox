<section>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-6">
        <div class="content">
          <div class="form-vertical">
            {% form 'reset_customer_password' %}

              <h1 class="text-center">{{ 'customer.reset_password.title' | t }}</h1>

              <p class="text-center">{{ 'customer.reset_password.subtext' | t: email: email }}</p>

              {{ form.errors | default_errors }}

              <div class="form-group">
                <label for="ResetPassword">{{ 'customer.reset_password.password' | t }}</label>
                <input type="password" value="" name="customer[password]" id="ResetPassword" class="{% if form.errors contains 'password' %} input--error{% endif %} form-control">
              </div>

              <div class="form-group">
                <label for="PasswordConfirmation">{{ 'customer.reset_password.password_confirm' | t }}</label>
                <input type="password" value="" name="customer[password_confirmation]" id="PasswordConfirmation" class="{% if form.errors contains 'password_confirmation' %} input--error{% endif %} form-control">
              </div>

              <input type="submit" class="button" value="{{ 'customer.reset_password.submit' | t }}">
            {% endform %}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
