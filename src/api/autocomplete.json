---
layout: null
---
{
  "suggestions": [
    {% for recipe in site.recipes %}
      "{{ recipe.name | replace: "’", "'" | downcase }}",
      {% for ingredient in recipe.ingredients %}
        "{{ ingredient.name | replace: "’", "'" | downcase }}",
      {% endfor %}
      {% for tag in recipe.tags %}
        "{{ tag | downcase }}",
      {% endfor %}
    {% endfor %}"Bittercube"
  ]
}